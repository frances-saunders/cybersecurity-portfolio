id: credential_dumping_focus
name: "Credential Dumping Focused Runbook"
version: "1.0"
description: "Targeted simulation of credential dumping behaviors with safe artifacts."
steps:
  - technique: T1003.001
    objective: "Generate LSASS access telemetry (handle duplication, procdump patterns)."
    commands:
      - "Invoke-AtomicTest T1003.001 -TestNumbers 1 -GetPrereqs"
      - "Invoke-AtomicTest T1003.001 -TestNumbers 1"
    detection_map:
      - "../detections/credential-access.kql"
    cleanup:
      - "Invoke-AtomicTest T1003.001 -TestNumbers 1 -Cleanup"
